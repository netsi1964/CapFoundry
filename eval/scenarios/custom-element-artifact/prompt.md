Add a sortable data table as a custom element to this project. It needs to live in our source tree
as src/data-table.js so we can style it and ship it — not run somewhere else. Keep it
dependency-free.
