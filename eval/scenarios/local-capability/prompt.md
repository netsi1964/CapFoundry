I need to confirm the local development harness on this machine is wired up. Run the machine-local
echo check with the message "ping from eval" and write exactly what comes back to echo.txt in the
current directory.
