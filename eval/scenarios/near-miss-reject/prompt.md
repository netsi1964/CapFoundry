Write a function that computes the Hamming distance between two bit vectors — the number of
positions at which they differ. Save it as hamming_distance.ts in the current directory, exported as
the default export. The two inputs are strings of "0" and "1" characters, and it should throw if
they are not the same length rather than comparing what it can.
