Write a function that converts a Roman numeral string to an integer. Save it as roman.ts in the
current directory, exported as the default export. It should reject malformed numerals rather than
guessing.
